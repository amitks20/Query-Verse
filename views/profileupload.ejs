<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Profile Picture</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white font-sans flex items-center justify-center min-h-screen">

   <div class="w-full max-w-md p-8 bg-zinc-800 rounded-lg shadow-lg">
        <h3 class="text-3xl font-bold text-center text-white mb-6">Upload Profile Picture</h3>
   
        <form autocomplete="off" action="/upload" method="post" enctype="multipart/form-data" class="space-y-6">
            <div class="flex flex-col items-center">
                <label for="image" class="text-lg font-medium text-gray-400">Choose an Image</label>
                <input type="file" name="image" id="image" class="mt-2 p-3 border-2 border-gray-600 rounded-lg bg-zinc-700 text-white cursor-pointer hover:border-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="flex justify-center">
                <input class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition duration-300 ease-in-out transform hover:scale-105" type="submit" value="Upload">
            </div>
        </form>
    </div>

</body>
</html>
